# Agent Instructions

When making UI, frontend, interaction, layout, styling, or accessibility changes, read and follow:

- `docs/UI_DESIGN_GUIDELINES.md`

Prefer the semantic CSS tokens in `src/renderer/src/styles.css` over hardcoded visual values where possible.

Keep the app style dense, quiet, tool-oriented, and consistent with the existing Electron media-library UI.
