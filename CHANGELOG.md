# Changelog

## 0.6.0 — 2026-08-31

### 多媒体库与首页

- 支持创建多个命名媒体库；来源目录、扫描策略、默认刮削器和排序按库独立保存，封面比例统一跟随全局外观设置
- 新增首页与跨库搜索，集中展示随机发现、近期添加、媒体库状态，并对共享影片去重
- 原媒体库列表拆分为独立路由和导航项，库内筛选、详情返回、滚动位置、批量任务和资源操作均保留媒体库上下文
- 全局影片资料由多个媒体库共享，成员关系、影片资源、扫描结果、待确认项和清理任务按库隔离
- 新增移出媒体库、跨库移动资源和全局删除的影响预览与并发保护；旧单库数据启动时无损迁入默认媒体库
- 媒体库设置并入全局设置工作区；扫描审计可按次查看新增、跳过、清理和待确认结果
- 扫描完成后的无资源清理改为移出媒体库成员，不再显示为删除影片
- 详情页删除影片与媒体库批量删除会删除本地视频和 STRM 源文件，避免再次扫描后自动加回

### Agent 平台

- 插件开发助手切换为“Javdex 产品控制面 + Pi `0.84.2` 数据面”，同一 run 长期复用一个 Pi AgentSession，并删除手写 loop、transcript 压缩与 provider tool-chat runtime
- 新增统一 Model/Route/Profile 配置、稳定 cache affinity、CredentialLease、ToolHost 权限/审批/锁/ledger，以及 ProductJournal、ExecutionHistory 与恢复记录
- PluginDevPanel 支持 renderer reload 与应用重启恢复，9 个工具（3 个自定义 + 6 个 Pi 原生）统一经过 ToolHost；增加只读 LibraryCurator 作为第二个 Agent 复用证明
- 影片和演员详情支持 Agent 辅助元数据采集，草稿写入待确认后再应用
- Electron 升级到 `43.4.1`（内置 Node 满足 Pi 要求），`better-sqlite3` 升级到 `13.0.3`

### 外部清单导入

- 清单页新增“导入外部清单”，统一支持新建清单和追加到已有清单；Agent 会完整读取普通分页、“加载更多”和可验证的有限虚拟滚动列表，并在写入前展示结果预览
- 已有影片按全局唯一番号复用；同番号存在多条记录时用详情页强身份信号消歧，仍有歧义时由用户查看发行商、日期、所属媒体库、资源和相关链接后选择
- 未匹配条目可按选项在目标媒体库创建无资源影片；影片详情页和外部清单页可分别选择是否保存到对应相关链接
- 清单可以跨媒体库引用影片；目标媒体库只决定新建影片写入位置，以及重复候选的媒体库偏好
- 被任意清单引用的无资源影片不会被扫描后自动清理；清单详情新增影片资源类型筛选

### 修复

- 打包安装后刮削不再因缺少 `undici` 失败；安装包显式保留 Cheerio 的完整运行时依赖链（包括 `undici`），并增加打包产物校验

### 升级说明

- 首次启动会自动将媒体库数据库从已发布的 schema 13 一次升级到 schema 14；已有影片、资源、分类关系和设置会保留
- 旧单库路径、扫描配置和资源会迁入默认媒体库；全局影片资料不会被复制
- 本版本不提供应用内自动安装更新，各平台均需重新下载对应安装包

## 0.5.0 — 2026-08-17

### 影片资源与扫描

- 统一管理本地文件、HTTP/HTTPS 视频直链、网页链接、Magnet 与 ED2K 资源，支持资源类型筛选、卡片标签、主资源切换、显示名称和标称大小维护
- 新增自动媒体库扫描、互斥任务协调、离线目录保护、安全缺失清理，以及移除媒体库路径后的延迟资源记录清理
- 支持扫描并持续同步 `.strm` 文件；目标变化会保留资源身份和影片归属，无效文件独立记录且不阻断其它安全清理
- 扫描开始、进度、完成与失败状态统一同步到前台，定时扫描后会刷新媒体库、分类、待确认数据和设置摘要

### 重复番号与待确认

- 允许多部影片共享番号，以永久内部 ID 标识记录，并以发行商、规范化番号和发行日期组成完整业务身份
- 扫描遇到同番号归属歧义时保存待确认资源组；影片刮削可保留多个精确候选，不再默认采用搜索首项
- 新增统一待确认中心，集中处理扫描资源、影片刮削和演员名称冲突，并保留跨重启恢复能力
- 支持影片合并、非主资源拆分、主资源保护和冲突预检；本地文件与链接导入均需明确选择现有影片或新建影片

### 分类资料

- 将制作商、发行商、导演和系列升级为稳定分类实体，支持主名、别名、简介、正式图片及扩展资料维护
- 支持机构跨角色复用与合并、导演和系列合并、删除影响预览，以及系列父子关系维护
- 刮削匹配统一按分类实体解析名称与别名，遇到歧义时要求显式确认，避免关联偏移
- 分类列表与详情支持按发行时间浏览关联影片，并改进搜索选择器、名称编辑和详情路由约束

### 刮削

- 新增 MetaTube 内置影片插件，连接用户自建服务端；未配置时禁止设为默认源、加入组合或执行刮削
- MetaTube 可选 Token 使用独立系统凭证保护存储，插件 Worker 只能经主进程受控服务桥访问 JSON API
- 支持服务地址、反向代理路径、独立刮削代理开关和连接测试；远程 HTTP 与 Token 组合保存时要求风险确认
- 多 Provider 精确候选进入待确认中心；MetaTube 演员统一按女性导入，`label` 近似映射为发行方
- 批量刮削按间隔回收浏览器会话，空字段任务会明确拒绝并清理检查点，避免长任务逐渐变慢或遗留幽灵状态

### 资料维护与界面

- 影片、演员和播放清单支持维护相关网页链接，并通过统一安全入口打开外部地址
- 演员库改为分页和虚拟化加载，完整保留筛选、Shift 连选、批量刮削及无关联演员删除能力
- 拆分共享领域类型、应用服务、媒体资产与刮削边界，补充类型化 IPC、数据库约束和跨层架构检查
- 渲染端样式逐步迁移到 CSS Modules 与视觉原语，并增加 CSS 架构、TypeScript、编码和领域边界门禁

### 修复与安全

- 修复 MetaTube 将连字符、下划线、内部空格或全角字符误判为同一番号的问题
- 修复定时扫描结束后前台仍显示旧媒体数据，以及分类详情路径可组合出错误实体页面的问题
- 图片探测仅在写入媒体资产时执行，资源路径生成避免同一时刻碰撞，并加强链接展示与隐私脱敏
- 更新网络请求与媒体解析依赖的安全修复版本

### 升级说明

- 首次启动会自动将媒体库数据库升级到 schema 13；已有影片 ID、资源、分类关系和设置会保留
- MetaTube 为可选刮削源，不内置服务端；使用前需在设置中填写自己的服务地址，可按需配置 Token
- `.strm` 会在后续媒体库扫描中自动识别并纳入资源生命周期；扫描不会访问远端探测内容
- 本版本不提供应用内自动安装更新，各平台均需重新下载对应安装包

## 0.4.0 — 2026-08-02

### 演员名称与冲突处理

- 建立标准化名称唯一归属，演员主名、别名及中英文名统一参与冲突判断
- 批量刮削遇到名称冲突时保留待确认结果，可在工作台中重新归属、修改名称、合并演员或将本次返回判定为非演员名称
- 名称归属调整、演员合并和待确认结果应用均采用原子写入，避免产生部分更新

### 演员库

- 演员卡片支持 Shift 连选，并可对选中演员批量刮削元数据
- 选中项全部无关联影片时，可原子批量删除演员档案并清理头像与写真
- 演员刮削状态统一为未刮削、成功和失败三种累计状态，并兼容恢复旧版批量任务
- 新增状态、头像和无人脸筛选；头像检测支持重试、缓存和批量扫描
- 支持预览头像源图，刷新头像后自动清理失效裁剪状态

### 刮削

- 新增可选内置演员头像源 Gfriends，支持主名优先、别名回退和质量排序选图
- 插件 `fetchBuffer` 支持带 ETag、容量限制和故障回退的持久资源缓存
- 演员组合刮削按字段源隔离故障，单个来源失败不再阻断其他字段
- 统一影片 15 个字段在空字段补齐、有值覆盖和覆盖更新下的语义，集合、站点元数据与图片资源按字段边界安全更新
- 封面和样张写入前验证本地资源；下载失败时保留旧图片，样张不再出现部分替换
- Cloudflare 验证通过后继续原刮削流程，不再因等待超时直接退出
- Xslist 默认匿名头像按无头像处理；无效演员写真不再写入资源库
- 移除内置影片刮削插件 JAV8

### 媒体库与图片预览

- 扫描器支持导入符号链接指向的影片文件，并区分删除库记录、链接和源文件的操作
- 图片未缩放时可通过鼠标拖动平滑切换上一张或下一张
- 改进演员名称搜索、筛选面板、批量任务状态和名称冲突界面的提示与交互

## 0.3.2 — 2026-07-26

### 隐私与界面

- 新增防窥模式：可按封面、样例、写真、头像等范围模糊敏感图片，并独立控制图片预览是否可用
- 媒体库支持 Shift 连选海报，便于批量操作

### 刮削

- 刮削下载图片优先复用刮削窗口已加载的图片响应，减少二次请求与防盗链失败
- 移除内置女优插件「偶像档案库」，升级内置 Xslist；旧设置中的引用会自动迁移
- 禁止自定义插件覆盖内置插件同名；旧版已覆盖的安装会重命名为 `-custom` 并同步设置引用

### 插件开发

- AI 调试载入内置插件时改为可安装的草稿副本，避免覆盖内置插件
- 支持导出完整 Agent 工作日志（含未截断工具输出），便于分析开发流程

### 网站与打包

- 更新项目网站与 README，并在 Release 后部署 GitHub Pages
- 修复 macOS 应用签名校验相关问题

## 0.3.1 — 2026-07-15

### 版本更新

- 新增“关于”页面，通过公开 GitHub Release 检测新版本，仅提醒并由用户前往下载，不执行自动更新
- 设置概览显示新版本提醒，支持直接忽略当前版本
- 支持在应用内展开并渲染 Release Log

### 智能头像构图

- 引入本地 MediaPipe 人脸检测、关键点与头发分割模型，不上传头像图片
- 头像编辑器支持智能构图，可选择脸部或头部居中、调整构图范围，并按需保护完整头部
- 设置页使用真实模型预览构图效果，缓存样张检测结果，并采用编辑、保存、取消流程
- 新增“构图全部头像”，提供进度、停止、完成汇总及统一任务日志
- 演员单次和批量刮削可选择自动智能构图；每位演员头像保存后立即构图，完成后再继续下一位
- 批量智能构图与影片、演员批量刮削强制互斥，避免并发改写头像

### 头像资源

- 保存头像原图、裁剪参数与源图指纹，重新构图不再依赖低清晰度展示图
- 改进旧头像迁移及 JPEG、PNG、GIF、WebP、AVIF 格式识别，修复扩展名与实际格式不一致导致的批量失败
- 提高头像导出清晰度，并在源图不变时保留已有手动裁剪

### 图片预览

- 修复 macOS 下图片预览窗口闪现后无法打开的问题
- 保留系统后退、鼠标返回键和 macOS 返回手势关闭预览的交互

### 文档与打包

- 补充版本规范、发布流程、图片预览历史栈及智能头像构图设计文档
- 打包本地模型与许可证文件，并在生产构建中校验模型运行资源完整性

## 0.3.0 — 2026-07-11

### 演员头像

- 头像源图与裁剪参数持久化（schema v2：`avatar_source_path` / `avatar_crop_json`）
- 头像资源路径按演员 ID 区分，避免同名演员共用文件
- 刮削下载与现有源图一致时保留手动裁剪，不覆盖
- 头像编辑器支持封面/写真取源，并改进加载与错误反馈

### 路由与导航

- 侧栏跨一级列表时恢复离开前的筛选；再点当前列表不再清空筛选
- 从影片详情进入厂牌/系列等分类时，不再带上媒体库筛选参数

### 界面

- 设置工作区、批量任务控件与数字步进器
- 统一空状态高度：固定面板铺满，详情样张/写真用中等带高
- 详情操作栏、列表表面与选择工具条等组件整理

### 修复

- 将 `strtok3` 声明为直接依赖，稳定影片时长探测

### 文档

- 补充主导航记忆、空状态高度与 UI 组件约定

## 0.2.2 — 2026-07-05

### 修复

- 安装版刮削失败：Worker 沙箱无法解析 `cheerio` 模块（`Cannot find module 'cheerio'`）

## 0.2.1 — 2026-07-05

### 体验

- 未刮削引导：设置概览通知、媒体库/演员列表维护 Banner、详情页提示
- 设置概览「一键刮削」入口

### 数据同步

- 批量刮削进度全局同步至列表与概览统计
- 优化路由切换时的 refetch 策略，减少侧栏切换多余请求

### 刮削

- Cloudflare 挑战页检测更准确，减少 JavDB 正常页误判
- 挑战页工具栏新增「保存页面」

### 修复

- 筛选弹层内下拉（刮削状态、年份等）点击无效
- 无可用 LLM 提供商时默认模型选项置空

### 文档

- README 重写，补充功能说明与界面截图

## 0.2.0 — 2026-07-05

预发布版本（非正式 1.0）。

### 媒体库

- 影片扫描、番号解析、多文件关联（`video_files`）
- 海报墙虚拟滚动、多维筛选与分类浏览
- 播放清单
- 样张导入（本地文件 / 图片链接）
- 详情页元数据展示、维护信息、外部评分

### 刮削

- 插件化刮削体系（内置 JavDB、JavLibrary、JAV8 及演员插件）
- 影片默认刮削插件：JavDB；演员默认：Xslist
- 批量刮削队列与字段级更新模式
- 刮削浏览器会话、挑战页处理、UA 配置
- 用户自定义插件与插件开发助手（含 MCP）

### 演员

- 演员库、别名归并、写真导入
- 演员刮削与详情页

### 设置与网络

- 媒体库路径、资产存储迁移
- HTTP/HTTPS 代理与连接测试
- LLM 多提供商配置与元数据翻译

### 数据

- SQLite schema version 1（无增量迁移；旧开发库需删除后重建）
- 设置持久化于 `settings.json`

### 其他

- 调用系统默认播放器，无内置播放器
- `media://` 本地资产协议

### 修复与优化

- 清除元数据时一并删除外部站点评分
- 导入弹窗：空状态占满剩余高度、不预留滚动条槽位
- 写真空状态高度与样张对齐
