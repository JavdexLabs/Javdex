# Changelog

## 0.6.2 — 2026-09-08

### 头像人脸检测

- 修复跨域头像未以 CORS 模式加载，导致 ImageBitmap 无法传入识别线程、无人脸筛选与批量智能构图全部失败的问题
- 全部识别失败时显示失败原因并保留原筛选，不再提示“识别完成”或应用空的无人脸结果

### 插件开发助手

- Gfriends 标记为不可调试，从开发助手插件下拉框隐藏，并禁用插件卡片的 AI 调试入口
- 修复模型已就绪但未填写网站主页时，任务提示错误显示“尚未配置可用模型”的问题；准确提示缺少的网站信息，保留模型不可用时的实际原因

### 待确认工作区

- 顶部改为标题与数量、分类与筛选两行布局，统一工作区标签与键盘导航；媒体库筛选独立定宽，窄窗口整体换行，移除重复零值徽标
- 空状态按分类说明用途，指定媒体库无结果时可一键查看所有媒体库
- 番号冲突改为先选择文件名或 NFO 番号、预览影响后统一确认；丢弃待办需明确确认
- 统一对象标题、原因说明和单行底部确认栏，低频操作收进“更多处理”，支持键盘聚焦与 Escape 关闭
- 媒体库筛选仅在扫描分类显示；提高辅助文字可读性，统一单选状态并减少卡片嵌套

### 设置页交互与样式

- 移除八个设置分类重复的大标题、简介和通用作用域提示；保留媒体库对象信息，将模型生效时机与代理测试说明移至对应设置旁
- 修复媒体库保存栏、图标与颜色标题间距；功能开关统一复用 Switch，多项选择复用 Checkbox，保留原有保存时机
- 补充表单间距与控件使用规范，增加 UI 控件静态检查，防止业务页面重新引入裸复选框或自制开关
- 组合插件入口增加按字段组合来源的说明与例子；创建弹窗支持一键应用某个插件到其支持字段、撤销应用和逐字段调整，保留未支持字段原设置
- 组合编辑补齐名称、可用来源与字段分配校验；失效来源可见并提示修复，窄窗口固定保存操作并独立滚动字段内容

- 网络代理改为紧凑横向表单，连接测试使用固定高度反馈栏，完整错误在详情弹窗展示，避免结果出现时卡片跳高
- 头像构图缩小预览、统一分段控件高度并细化滑块与复选框，参数区按设置行对齐；移除对应旧全局样式

- 统一分类标题、作用域说明和子页签；AI 模型、影片/演员刮削来源、图片资源/资料导出支持 URL 恢复和前进后退
- 代理地址与连接方式一起显式保存；测试当前输入不会修改配置，分别展示两类代理的保存和测试状态
- 新增设置草稿、分组保存/取消和离开保护，外部刷新保留编辑，保存期间的新输入不会被旧结果覆盖
- 模型运行参数默认收起，继承默认模型显示实际结果；未配置时引导添加提供商与模型，连接弹窗补齐离开确认和密钥显示控制
- 即时设置与待保存草稿明确区分；数字步进器支持完整数字输入、键盘增减和可见范围错误；主题保存失败恢复上次成功配置
- 图片目录先选择目标再确认迁移；图片加解密改为明确动作按钮；取消选目录不再提示成功
- 空媒体库设置可直接新建，插件列表取消内层滚动；更新和导出选项加载失败可重试，导出偏好保存失败单独提示
- 侧栏仅展示活动媒体库；已归档媒体库从普通导航隐藏，仍可在媒体库设置中恢复

### 扫描异常处理

- “重命名并导入”不再要求手动填写番号或选择目标；重命名后按媒体库扫描规则重新识别，同番号沿用自动归属或待确认流程
- 重新识别失败时保留新文件名并更新待处理项路径，支持继续修改；只重新识别当前文件

### NFO 完整性与性能

- 修复 Plex/Infuse 格式同时导出背景和样张后，本地 NFO 读取仅保留背景而漏掉样张的问题；补查仍限定当前影片文件名前缀
- 修复新建媒体库和保存本地 NFO 导入设置时，IPC 校验遗漏 `autoImportLocalNfo` 导致请求被拒绝的问题
- 新建媒体库向导补齐“本地元数据 → 自动导入本地 NFO”，与媒体库设置页保持相同说明和默认值
- 本地 NFO 样张按影片引用和文件名前缀归属，避免同目录影片混入彼此图片；扫描复用本次目录索引，避免逐视频重复遍历
- 导出图片分别校验内容，单张失败不再使 NFO 和其它图片失效；图片读取次数随资产数量线性增长
- 导出预览按媒体库根与物理目录复用路径检查，并复用锚点文件状态，避免 NFO、封面和附件重复访问网络目录
- 同目录影片可共享相同演员头像，真正冲突的头像不写入且从相关 NFO 引用中移除
- 身份待确认资源变化后，重新扫描会刷新文件和 STRM 目标快照，保留用户确认步骤

### NFO 前台导出

- 本地 NFO 插件卡片移除标题中的重复“内置”标记，版本号统一显示为 `1.0.0`
- 导出界面明确用途和必选资料，常用图片与可选附件分组，详细规则按需展开；预览按实际写入动作显示文件组成，补齐设置变化、无需写入和部分完成提示
- 终止导出失败时在任务弹窗内显示原因并允许重试，键盘焦点保持在弹窗中，关闭报告后返回预览操作
- 演员头像按 Kodi 约定将文件名空格替换为下划线，NFO 引用同步更新且保留原姓名；沿用默认关闭与冲突保护，界面明确本地头像兼容边界
- 样张收窄为默认关闭的“附带样张文件（备份/迁移）”，保存至独立 `javdex-samples` 目录，不再写入 NFO 背景引用；Javdex 优先回读本影片备份样张，并继续兼容旧 `extrafanart` 布局
- 横版封面导出完整 `landscape` 图片和从右侧裁剪的 2:3 海报；竖版只导出海报，支持 EXIF 方向校正，原始资产不变
- 图片预览按实际产物计数和估算体积，批量规划逐图让出主线程；已有海报跳过、不同扩展名旧图和不可解码封面会明确提示
- 导出配置复用软件通用下拉框，精简统计并将操作放在预览之后；提示、文件明细与完成报告按需展开、分页显示，文件状态改用中文
- “设置 → 存储”新增一次性 NFO 导出：选择一个或多个媒体库和五种兼容 profile，先生成不可变计划并预览新建、覆盖、跳过、冲突、警告、样张数和预计体积，再确认执行
- 支持本地视频与 STRM 源文件的 exact-stem NFO、封面、详情背景、可选样张和演员头像；无本地锚点的链接资源单独计数并跳过
- 默认跳过已有文件；显式覆盖也会在逐项写入前复核来源快照、媒体根边界与目标 hash/mtime，变化项返回 `stale-plan`
- 正式导出使用全应用阻塞进度弹窗，允许明确终止并保留已原子完成文件；结束后在同一弹窗展示逐项报告
- 导出不后台运行、不跨重启恢复、不保存 manifest、不建立 NFO 关联、不发送系统通知，也不包含任何 Agent 能力
- 新增通用/Kodi、Jellyfin、Emby/Kodi 保守子集、Plex 官方 NFO Agent（PMS 1.43.1+）和 Infuse profile；兼容基线与限制见 `docs/NFO_COMPATIBILITY.md`

### 本地 NFO 一次性导入

- 媒体库扫描可在资源首次发现时读取相邻 NFO，并通过内置“本地 NFO”元数据来源补齐空字段；每个媒体库默认开启，也可在“扫描设置”中即时关闭
- 没有 NFO 是不产生警告或待办的正常情况；已有正式资源不重复读取 NFO，累计刮削成功的影片可借助 NFO 识别新资源但不会被自动覆盖
- 文件名与 NFO 番号冲突时进入专门的资源身份待确认，确认前不创建影片成员或正式资源；多份 NFO 沿用影片刮削候选确认，不自动合并字段
- 自动与手动导入只接收媒体库根目录内的安全本地图片；远程图片地址和越界引用会被忽略并记录普通警告，不保存 NFO 路径、摘要、时间或同步关系

### 升级说明

- 首次启动会自动将媒体库数据库从 schema 14 升级到 schema 15，新增媒体库级 NFO 自动导入配置和资源身份待确认记录；已有影片、资源、设置和插件保持不变

## 0.6.1 — 2026-09-01

### 外部清单导入

- 导入任务在 browser Session 和首轮对话就绪后立即进入 Agent 对话，不再等待来源页导航，配置页可以更快切换到运行状态
- Agent 可自行打开冻结来源、处理安全的同站点导航和页面交互，并在登录或人机验证 handoff 完成后主动返回冻结工作项
- 修复登录重定向、人机验证和页面恢复场景中，Agent 过晚请求用户操作或要求用户手动返回清单页的问题

### 安全与完整性

- 主 frame 跨站点导航改为在请求发出前阻断，避免点击、提交或键盘操作先访问未授权目标
- 清单分页继续由宿主原子认领和推进；通用 browser 操作不能绕过 frontier、检查点或最终应用约束
- browser 工具审计只保存脱敏路径、长度、计数和 digest，不记录查询参数、输入内容或页面表达式正文

### 升级说明

- 本版本不新增数据库迁移，直接安装新版本即可；已有媒体库、设置、清单和插件不会被删除
- 本版本不提供应用内自动安装更新，各平台均需重新下载对应安装包

## 0.6.0 — 2026-08-31

### 多媒体库与首页

- 支持创建多个命名媒体库；来源目录、扫描策略、默认刮削器和排序按库独立保存，封面比例统一跟随全局外观设置
- 新增首页与跨库搜索，集中展示随机发现、近期添加、媒体库状态，并对共享影片去重
- 原媒体库列表拆分为独立路由和导航项，库内筛选、详情返回、滚动位置、批量任务和资源操作均保留媒体库上下文
- 全局影片资料由多个媒体库共享，成员关系、影片资源、扫描结果、待确认项和清理任务按库隔离
- 新增移出媒体库、跨库移动资源和全局删除的影响预览与并发保护；旧单库数据启动时无损迁入默认媒体库
- 媒体库设置并入全局设置工作区；扫描审计可按次查看新增、跳过、清理和待确认结果
- 扫描完成后的无资源清理改为移出媒体库成员，不再显示为删除影片
- 详情页删除影片与媒体库批量删除会删除本地视频和 STRM 源文件，避免再次扫描后自动加回

### Agent 平台

- 插件开发助手切换为“Javdex 产品控制面 + Pi `0.84.2` 数据面”，同一 run 长期复用一个 Pi AgentSession，并删除手写 loop、transcript 压缩与 provider tool-chat runtime
- 新增统一 Model/Route/Profile 配置、稳定 cache affinity、CredentialLease、ToolHost 权限/审批/锁/ledger，以及 ProductJournal、ExecutionHistory 与恢复记录
- PluginDevPanel 支持 renderer reload 与应用重启恢复；3 个自定义工具统一经过 ToolHost，6 个 Pi 原生文件工具受冻结 allowlist、工作区路径/写入白名单与统一 ledger 约束；增加只读 LibraryCurator 作为第二个 Agent 复用证明
- 影片和演员详情支持 Agent 辅助元数据采集，草稿写入待确认后再应用
- Electron 升级到 `43.4.1`（内置 Node 满足 Pi 要求），`better-sqlite3` 升级到 `13.0.3`

### 外部清单导入

- 清单页新增“导入外部清单”，统一支持新建清单和追加到已有清单；Agent 会完整读取普通分页、“加载更多”和可验证的有限虚拟滚动列表，运行中展示收集进度和结果，并在身份确认完成后一次性写入
- 已有影片先按全局目录中的规范化番号查找；同番号存在多条记录时用详情页强身份信号消歧，仍有歧义时由用户查看发行商、日期、所属媒体库、资源和相关链接后选择
- 未匹配条目可按选项在目标媒体库创建无资源影片；影片详情页和外部清单页可分别选择是否保存到对应相关链接
- 清单可以跨媒体库引用影片；目标媒体库只决定新建影片写入位置，以及重复候选的媒体库偏好
- 被任意清单引用的无资源影片不会被扫描后自动清理；清单详情新增影片资源类型筛选

### 修复

- 打包安装后刮削不再因缺少 `undici` 失败；安装包显式保留 Cheerio 的完整运行时依赖链（包括 `undici`），并增加打包产物校验
- Agent 恢复载荷只写入加密执行历史，工具 ledger 仅保留脱敏参数和定长摘要；Pi 原生文件工具补齐冻结权限校验和统一审计记录
- 更新存在已知漏洞的构建依赖，并修复 macOS 双架构包的 ad-hoc 签名及跨架构运行时校验

### 升级说明

- 首次启动会自动将媒体库数据库从已发布的 schema 13 一次升级到 schema 14；已有影片、资源、分类关系和设置会保留
- 旧单库路径、扫描配置和资源会迁入默认媒体库；全局影片资料不会被复制
- 本版本不提供应用内自动安装更新，各平台均需重新下载对应安装包

## 0.5.0 — 2026-08-17

### 影片资源与扫描

- 统一管理本地文件、HTTP/HTTPS 视频直链、网页链接、Magnet 与 ED2K 资源，支持资源类型筛选、卡片标签、主资源切换、显示名称和标称大小维护
- 新增自动媒体库扫描、互斥任务协调、离线目录保护、安全缺失清理，以及移除媒体库路径后的延迟资源记录清理
- 支持扫描并持续同步 `.strm` 文件；目标变化会保留资源身份和影片归属，无效文件独立记录且不阻断其它安全清理
- 扫描开始、进度、完成与失败状态统一同步到前台，定时扫描后会刷新媒体库、分类、待确认数据和设置摘要

### 重复番号与待确认

- 允许多部影片共享番号，以永久内部 ID 标识记录，并以发行商、规范化番号和发行日期组成完整业务身份
- 扫描遇到同番号归属歧义时保存待确认资源组；影片刮削可保留多个精确候选，不再默认采用搜索首项
- 新增统一待确认中心，集中处理扫描资源、影片刮削和演员名称冲突，并保留跨重启恢复能力
- 支持影片合并、非主资源拆分、主资源保护和冲突预检；本地文件与链接导入均需明确选择现有影片或新建影片

### 分类资料

- 将制作商、发行商、导演和系列升级为稳定分类实体，支持主名、别名、简介、正式图片及扩展资料维护
- 支持机构跨角色复用与合并、导演和系列合并、删除影响预览，以及系列父子关系维护
- 刮削匹配统一按分类实体解析名称与别名，遇到歧义时要求显式确认，避免关联偏移
- 分类列表与详情支持按发行时间浏览关联影片，并改进搜索选择器、名称编辑和详情路由约束

### 刮削

- 新增 MetaTube 内置影片插件，连接用户自建服务端；未配置时禁止设为默认源、加入组合或执行刮削
- MetaTube 可选 Token 使用独立系统凭证保护存储，插件 Worker 只能经主进程受控服务桥访问 JSON API
- 支持服务地址、反向代理路径、独立刮削代理开关和连接测试；远程 HTTP 与 Token 组合保存时要求风险确认
- 多 Provider 精确候选进入待确认中心；MetaTube 演员统一按女性导入，`label` 近似映射为发行方
- 批量刮削按间隔回收浏览器会话，空字段任务会明确拒绝并清理检查点，避免长任务逐渐变慢或遗留幽灵状态

### 资料维护与界面

- 影片、演员和播放清单支持维护相关网页链接，并通过统一安全入口打开外部地址
- 演员库改为分页和虚拟化加载，完整保留筛选、Shift 连选、批量刮削及无关联演员删除能力
- 拆分共享领域类型、应用服务、媒体资产与刮削边界，补充类型化 IPC、数据库约束和跨层架构检查
- 渲染端样式逐步迁移到 CSS Modules 与视觉原语，并增加 CSS 架构、TypeScript、编码和领域边界门禁

### 修复与安全

- 修复 MetaTube 将连字符、下划线、内部空格或全角字符误判为同一番号的问题
- 修复定时扫描结束后前台仍显示旧媒体数据，以及分类详情路径可组合出错误实体页面的问题
- 图片探测仅在写入媒体资产时执行，资源路径生成避免同一时刻碰撞，并加强链接展示与隐私脱敏
- 更新网络请求与媒体解析依赖的安全修复版本

### 升级说明

- 首次启动会自动将媒体库数据库升级到 schema 13；已有影片 ID、资源、分类关系和设置会保留
- MetaTube 为可选刮削源，不内置服务端；使用前需在设置中填写自己的服务地址，可按需配置 Token
- `.strm` 会在后续媒体库扫描中自动识别并纳入资源生命周期；扫描不会访问远端探测内容
- 本版本不提供应用内自动安装更新，各平台均需重新下载对应安装包

## 0.4.0 — 2026-08-02

### 演员名称与冲突处理

- 建立标准化名称唯一归属，演员主名、别名及中英文名统一参与冲突判断
- 批量刮削遇到名称冲突时保留待确认结果，可在工作台中重新归属、修改名称、合并演员或将本次返回判定为非演员名称
- 名称归属调整、演员合并和待确认结果应用均采用原子写入，避免产生部分更新

### 演员库

- 演员卡片支持 Shift 连选，并可对选中演员批量刮削元数据
- 选中项全部无关联影片时，可原子批量删除演员档案并清理头像与写真
- 演员刮削状态统一为未刮削、成功和失败三种累计状态，并兼容恢复旧版批量任务
- 新增状态、头像和无人脸筛选；头像检测支持重试、缓存和批量扫描
- 支持预览头像源图，刷新头像后自动清理失效裁剪状态

### 刮削

- 新增可选内置演员头像源 Gfriends，支持主名优先、别名回退和质量排序选图
- 插件 `fetchBuffer` 支持带 ETag、容量限制和故障回退的持久资源缓存
- 演员组合刮削按字段源隔离故障，单个来源失败不再阻断其他字段
- 统一影片 15 个字段在空字段补齐、有值覆盖和覆盖更新下的语义，集合、站点元数据与图片资源按字段边界安全更新
- 封面和样张写入前验证本地资源；下载失败时保留旧图片，样张不再出现部分替换
- Cloudflare 验证通过后继续原刮削流程，不再因等待超时直接退出
- Xslist 默认匿名头像按无头像处理；无效演员写真不再写入资源库
- 移除内置影片刮削插件 JAV8

### 媒体库与图片预览

- 扫描器支持导入符号链接指向的影片文件，并区分删除库记录、链接和源文件的操作
- 图片未缩放时可通过鼠标拖动平滑切换上一张或下一张
- 改进演员名称搜索、筛选面板、批量任务状态和名称冲突界面的提示与交互

## 0.3.2 — 2026-07-26

### 隐私与界面

- 新增防窥模式：可按封面、样例、写真、头像等范围模糊敏感图片，并独立控制图片预览是否可用
- 媒体库支持 Shift 连选海报，便于批量操作

### 刮削

- 刮削下载图片优先复用刮削窗口已加载的图片响应，减少二次请求与防盗链失败
- 移除内置女优插件「偶像档案库」，升级内置 Xslist；旧设置中的引用会自动迁移
- 禁止自定义插件覆盖内置插件同名；旧版已覆盖的安装会重命名为 `-custom` 并同步设置引用

### 插件开发

- AI 调试载入内置插件时改为可安装的草稿副本，避免覆盖内置插件
- 支持导出完整 Agent 工作日志（含未截断工具输出），便于分析开发流程

### 网站与打包

- 更新项目网站与 README，并在 Release 后部署 GitHub Pages
- 修复 macOS 应用签名校验相关问题

## 0.3.1 — 2026-07-15

### 版本更新

- 新增“关于”页面，通过公开 GitHub Release 检测新版本，仅提醒并由用户前往下载，不执行自动更新
- 设置概览显示新版本提醒，支持直接忽略当前版本
- 支持在应用内展开并渲染 Release Log

### 智能头像构图

- 引入本地 MediaPipe 人脸检测、关键点与头发分割模型，不上传头像图片
- 头像编辑器支持智能构图，可选择脸部或头部居中、调整构图范围，并按需保护完整头部
- 设置页使用真实模型预览构图效果，缓存样张检测结果，并采用编辑、保存、取消流程
- 新增“构图全部头像”，提供进度、停止、完成汇总及统一任务日志
- 演员单次和批量刮削可选择自动智能构图；每位演员头像保存后立即构图，完成后再继续下一位
- 批量智能构图与影片、演员批量刮削强制互斥，避免并发改写头像

### 头像资源

- 保存头像原图、裁剪参数与源图指纹，重新构图不再依赖低清晰度展示图
- 改进旧头像迁移及 JPEG、PNG、GIF、WebP、AVIF 格式识别，修复扩展名与实际格式不一致导致的批量失败
- 提高头像导出清晰度，并在源图不变时保留已有手动裁剪

### 图片预览

- 修复 macOS 下图片预览窗口闪现后无法打开的问题
- 保留系统后退、鼠标返回键和 macOS 返回手势关闭预览的交互

### 文档与打包

- 补充版本规范、发布流程、图片预览历史栈及智能头像构图设计文档
- 打包本地模型与许可证文件，并在生产构建中校验模型运行资源完整性

## 0.3.0 — 2026-07-11

### 演员头像

- 头像源图与裁剪参数持久化（schema v2：`avatar_source_path` / `avatar_crop_json`）
- 头像资源路径按演员 ID 区分，避免同名演员共用文件
- 刮削下载与现有源图一致时保留手动裁剪，不覆盖
- 头像编辑器支持封面/写真取源，并改进加载与错误反馈

### 路由与导航

- 侧栏跨一级列表时恢复离开前的筛选；再点当前列表不再清空筛选
- 从影片详情进入厂牌/系列等分类时，不再带上媒体库筛选参数

### 界面

- 设置工作区、批量任务控件与数字步进器
- 统一空状态高度：固定面板铺满，详情样张/写真用中等带高
- 详情操作栏、列表表面与选择工具条等组件整理

### 修复

- 将 `strtok3` 声明为直接依赖，稳定影片时长探测

### 文档

- 补充主导航记忆、空状态高度与 UI 组件约定

## 0.2.2 — 2026-07-05

### 修复

- 安装版刮削失败：Worker 沙箱无法解析 `cheerio` 模块（`Cannot find module 'cheerio'`）

## 0.2.1 — 2026-07-05

### 体验

- 未刮削引导：设置概览通知、媒体库/演员列表维护 Banner、详情页提示
- 设置概览「一键刮削」入口

### 数据同步

- 批量刮削进度全局同步至列表与概览统计
- 优化路由切换时的 refetch 策略，减少侧栏切换多余请求

### 刮削

- Cloudflare 挑战页检测更准确，减少 JavDB 正常页误判
- 挑战页工具栏新增「保存页面」

### 修复

- 筛选弹层内下拉（刮削状态、年份等）点击无效
- 无可用 LLM 提供商时默认模型选项置空

### 文档

- README 重写，补充功能说明与界面截图

## 0.2.0 — 2026-07-05

预发布版本（非正式 1.0）。

### 媒体库

- 影片扫描、番号解析、多文件关联（`video_files`）
- 海报墙虚拟滚动、多维筛选与分类浏览
- 播放清单
- 样张导入（本地文件 / 图片链接）
- 详情页元数据展示、维护信息、外部评分

### 刮削

- 插件化刮削体系（内置 JavDB、JavLibrary、JAV8 及演员插件）
- 影片默认刮削插件：JavDB；演员默认：Xslist
- 批量刮削队列与字段级更新模式
- 刮削浏览器会话、挑战页处理、UA 配置
- 用户自定义插件与插件开发助手（含 MCP）

### 演员

- 演员库、别名归并、写真导入
- 演员刮削与详情页

### 设置与网络

- 媒体库路径、资产存储迁移
- HTTP/HTTPS 代理与连接测试
- LLM 多提供商配置与元数据翻译

### 数据

- SQLite schema version 1（无增量迁移；旧开发库需删除后重建）
- 设置持久化于 `settings.json`

### 其他

- 调用系统默认播放器，无内置播放器
- `media://` 本地资产协议

### 修复与优化

- 清除元数据时一并删除外部站点评分
- 导入弹窗：空状态占满剩余高度、不预留滚动条槽位
- 写真空状态高度与样张对齐
