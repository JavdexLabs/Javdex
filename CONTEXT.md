# Javdex 媒体库

本上下文描述 Javdex 媒体库中的影片、演员及其元数据身份，以及应用内 Agent 与模型配置的核心概念。

## Language

**Agent 定义**：
随应用发布的一类 AI 工作流约定，声明输入、结果、模型角色与可用工具包；它不是一次具体执行，完成条件由对应具体用例解释。
_Avoid_：Agent 实例、Pi 会话、提示词模板

**Agent 运行时**：
负责执行模型与工具循环的数据面引擎；它承载一次 Agent 运行，但不拥有该运行的产品状态或领域完成判定。
_Avoid_：Agent 运行、Pi session、插件开发工作流

**Agent 配置方案**：
将一个 Agent 定义绑定到模型角色、工具授权、审批规则、缓存偏好与资源预算的可版本化配置；同一 Agent 定义可以有多套配置方案。
_Avoid_：全局 Agent 设置、模型预设、运行实例

**Agent 运行**：
由一个 Agent 配置方案创建的一次可识别执行生命周期；它拥有稳定 ID、状态、事件历史与固定的定义和配置版本，可以等待、继续或取消。
_Avoid_：Pi session、对话、一次模型请求

**Agent 工具包**：
按领域能力聚合并版本化的一组受信任工具声明与执行规则；Agent 只能使用其配置方案明确授予的工具。
_Avoid_：Pi extension、全局工具集合、无限制主进程能力

**模型角色**：
Agent 定义中命名的模型用途，例如 primary、verifier 或 summarizer；每次 Agent 运行通过 AI 路由为角色解析具体模型。
_Avoid_：具体模型、模型预设、供应商

**模型能力**：
一个具体模型有证据支持的调用特性与限制，例如工具调用、结构化输出、视觉输入或上下文窗口。
_Avoid_：供应商兼容性、工具授权、宿主能力

**工具授权**：
Agent 配置方案在 Agent 定义声明范围内允许使用的工具与操作效果；审批只能批准既有授权，不能扩大它。
_Avoid_：模型能力、宿主能力、prompt 约束

**宿主能力**：
Electron 主进程向工具实现提供的受控应用能力，例如浏览器检查、插件写入或媒体库查询。
_Avoid_：整个主进程对象、工具授权、模型能力

**模型连接**：
一个可配置的模型服务端点、模型传输类型与凭据引用；同一厂商可以存在多个连接，连接本身不代表具体模型。
_Avoid_：供应商即连接、API Key、模型

**模型记录**：
隶属于一个模型连接的具体模型身份，以及有来源依据的能力和限制信息。
_Avoid_：供应商级 Agent 兼容标记、模型显示名

**模型预设**：
一个模型记录与调用参数的可复用组合，用于固定温度、输出上限、思考等级和缓存保留期等调用偏好。
_Avoid_：模型连接、AI 路由、Agent 配置方案

**AI 路由**：
一个工作负载或 Agent 模型角色到有序模型预设的显式选择规则，并声明必须满足的模型能力；没有配置时不得静默改用任意可用模型。
_Avoid_：全局默认模型、首个可用模型、隐式回退

**影片**：
媒体库中一部独立发行作品的资料记录；身份由永久的内部影片 ID 确定，番号相同不代表是同一部影片。
_Avoid_：番号即影片、影片资源、同番号影片必然合并

**影片业务身份**：
发行商、番号与发行日期共同构成的影片唯一身份；三项完整时在媒体库内只能归属一部影片，其中发行商按分类实体身份而非显示名称判断。
_Avoid_：番号唯一键、发行商名称键、制作商与发行商混用

**规范化影片番号**：
影片番号移除首尾空白并转为大写后的身份比较值；连字符、下划线、内部空格及尾缀均保留，只有规范化后逐字符相等才属于番号完全匹配。
_Avoid_：包含匹配、忽略分隔符、模糊番号、搜索首项回退

**影片番号纠正**：
用户修改一部影片规范化番号的操作；同番号影片可以并存，纠正本身不合并影片，形成完整影片业务身份冲突时须另行修正身份或显式合并。影片有待确认影片刮削结果时，须经用户确认并原子清理该结果及暂存资源后才能修改番号。
_Avoid_：改番号即合并、番号占用、隐式资源迁移

**身份待定影片**：
发行商、番号或发行日期尚不完整，因而无法确定完整影片业务身份的影片；身份待定影片可以与同番号影片并存。
_Avoid_：重复影片、非法影片、临时影片

**影片身份冲突**：
影片补全或修改业务身份后，与另一部影片形成相同完整业务身份的待处理状态；系统不得静默覆盖资料，须由用户合并影片或修正身份字段。手动编辑冲突不保存拟写入值，进入普通影片合并时丢弃整张编辑表单，取消则保持原身份；发行商实体合并会制造冲突时须阻止机构合并，先处理受影响影片。
_Avoid_：写入失败、番号冲突、自动覆盖

**扫描资源自动归并**：
媒体库扫描按番号为一组新发现的本地影片资源与 STRM 影片资源选择唯一现有影片，或共同创建一部身份待定影片的策略；同番号现有影片多于一部时不推测归属，而是等待用户确认。开关默认开启且只影响启用后新发现的资源，重新开启不会自动处理既有待确认扫描组。
_Avoid_：自动合并影片、随机选择同番号影片、按番号消除影片记录

**手动影片资源导入**：
用户主动添加本地或链接影片资源的操作；已有同番号影片时始终由用户明确选择目标影片或新建身份待定影片，不受扫描资源自动归并开关影响。用户为未识别番号的有效 STRM 源文件补充番号或重命名后，形成的仍是 STRM 影片资源而非普通手动链接。
_Avoid_：手动导入自动归并、任取同番号影片、扫描待确认组

**待确认扫描资源**：
扫描已发现但尚未分配给任何影片的视频文件或有效 STRM 源文件；确认归属前它还没有形成影片资源，也不进入普通媒体库。
_Avoid_：待确认影片、未归属影片资源、临时影片

**STRM 待确认快照**：
待确认扫描资源为 STRM 源文件时，用户所见目标是待确认快照的一部分；提交前源文件须重新读取校验，目标已变化则拒绝原提交并刷新待确认项，不静默采用新目标或写入旧目标。
_Avoid_：陈旧链接提交、静默采用新链接、确认后再同步

**待确认扫描组**：
扫描资源自动归并关闭或无法唯一确定目标影片时，同一规范化番号下等待用户完整分配的一组待确认扫描资源；确认须原子提交，不支持部分处理或顺带合并既有影片，新建影片分组默认采用扫描主资源选择且允许用户覆盖。后续安全扫描按各类源文件身份去重并刷新资源，只移除可访问目录中确认缺失的资源，离线目录中的资源继续保留，空组自动移除；本期不提供永久忽略规则。
_Avoid_：待确认影片、临时影片、未保存扫描结果、部分处理

**分类实体**：
机构、导演或系列的稳定资料记录；身份由实体 ID 确定，主名变化不会改变身份或影片关联。
_Avoid_：自由文本分类、名称即身份、临时分类项

**机构角色**：
机构在媒体库中持久承担的制作商或发行商身份；同一机构可以同时拥有两个角色，角色不因当前关联影片数量变为零而自动消失。
_Avoid_：两份机构资料、影片数量推断角色、无影片即无角色

**分类名称**：
分类实体的主名与对用户可见别名的总称；机构名称在统一机构领域唯一归属，系列名称在所属机构作用域内唯一归属，导演名称允许跨导演重复。
_Avoid_：隐藏名称、机构角色专属名称、导演名称即唯一身份

**分类标准化名称**：
分类名称经过 Unicode NFKC 规范化、移除全部空白并转为小写后的身份匹配键；标点、繁简体、假名和罗马字差异保持不变，标准化后为空的文本不构成分类名称。
_Avoid_：模糊匹配键、自动译名、罗马字映射

**分类主图**：
应用拥有并由分类实体正式引用的机构品牌图、导演肖像或系列封面；没有分类主图时使用的关联影片封面只是动态展示回退。
_Avoid_：影片封面回退、外部热链、任意磁盘图片

**相关链接**：
用户为分类实体、影片、演员或清单维护的有序 HTTP/HTTPS 外部地址；每条有显示标签，同一记录内去除片段后的 URL 唯一，刮削不得写入或覆盖，清空影片或演员资料也不得移除。它不是影片资源，也不是影片站点元数据。
_Avoid_：来源链接、站点元数据、影片资源、全局来源链接

**系列归属**：
用户明确设置的系列所属机构；名称唯一作用域随归属确定，不根据影片的制作商、发行商或共同出现关系自动推断。
_Avoid_：制作商即所属机构、自动所有权、影片共现推断

**演员主名**：
演员当前唯一的规范显示名称。更换主名即替换此名称，旧值不构成别名或历史身份。
_Avoid_：历史主名、隐藏主名

**演员别名**：
为演员明确保留且对用户可见的替代名称；一个演员可以没有别名，也可以有多个别名。
_Avoid_：隐藏名称、隐式别名

**演员名称归属**：
演员主名、中文名、英文名和别名共同构成可搜索的演员名称；同一个标准化名称最多只能归属一位演员。
_Avoid_：共享演员名称、多演员名称归属

**标准化名称**：
演员名称经过 Unicode NFKC 规范化、移除全部空白并转为小写后的归属键；标点、繁简体、假名和罗马字差异保持不变，标准化后为空的文本不构成演员名称。
_Avoid_：模糊名称、自动译名、罗马字映射

**名称归属转移**：
用户明确将一个标准化名称从当前归属演员改归另一位演员；原演员名下该名称的全部名称类型同时失去归属，原演员主名被转移时必须先指定替代主名。
_Avoid_：别名复制、共享名称、自动抢占

**名称归属冲突**：
演员刮削返回的标准化名称已经归属于另一位演员；同一演员内部的名称类型重复不构成冲突。发生冲突时，整份刮削结果成为待确认演员刮削结果。
_Avoid_：同演员名称重复、字段写入错误

**名称冲突项**：
待确认演员刮削结果中一个尚未解决的名称归属冲突；它关联该结果中的原始名称、标准化名称及名称类型。
_Avoid_：冲突组状态、字段写入错误

**名称冲突聚合组**：
当前所有待确认演员刮削结果中标准化名称相同的名称冲突项集合；它是随冲突项变化而实时形成的视图，不是独立保存的长期记录。
_Avoid_：持久化冲突组、非法名称记录

**待确认名称归属**：
数据库升级时发现同一个标准化名称由多位演员保存而形成的未决归属；它保留各演员的原始名称和名称类型，但解决前不参与搜索或演员身份匹配，也不构成待确认演员刮削结果或改变刮削状态。
_Avoid_：自动归属、迁移刮削结果、随机名称命中

**演员头像源**：
按演员名称查找候选头像的外部数据源；仅负责提供头像，不作为演员身份、别名或完整资料的权威来源。
_Avoid_：演员资料源、完整演员刮削器

**无关联演员**：
当前没有关联任何影片的演员；删除时只移除演员档案及其媒体资源，不影响影片资源。
_Avoid_：空演员、孤立演员

**演员合并**：
用户确认两条演员档案代表同一人后，将其收敛为一条档案；用户明确选择最终保留演员和演员主名，关联资料与无冲突名称归入最终演员，相关链接按去片段 URL 取并集且保留侧优先。
_Avoid_：自动合并、资料改投、名称共享

**演员刮削状态**：
演员资料刮削的累计结果；“未刮削”表示尚无成功或失败记录，“刮削失败”表示尝试过但从未成功，“刮削成功”表示至少成功过一次且后续失败不会使其降级；待确认演员刮削结果不改变累计状态。
_Avoid_：最近刮削结果、刮削时间状态

**影片站点元数据**：
影片从一个具体刮削站点取得的来源链接与站点评分；不同站点的记录彼此独立，补齐、覆盖或清空一个站点的记录不得影响其他站点。
_Avoid_：全局来源链接、全局站点评分

**影片刮削集合字段**：
影片刮削中的女优、男优、刮削标签和样张各自构成一个整体集合；更新时按字段整体写入或清空，不与旧刮削集合合并，其中手动标签不属于刮削标签集合。
_Avoid_：增量合并、跨站点集合、手动标签覆盖

**媒体资源**：
位于媒体资源根目录内、由库拥有的本地图片文件（含明文与加密形态）；影片源视频文件不是媒体资源。
_Avoid_：影片源文件、任意磁盘图片

**影片资源**：
可用于访问一部影片内容的来源记录，包括本地文件、HTTP 直链、网页链接、Magnet 和 ED2K；它与由应用拥有的图片“媒体资源”是两个不同概念。
_Avoid_：媒体资源、影片图片、仅本地文件

**STRM 影片资源**：
由一个有效 STRM 源文件持续托管的链接影片资源；其访问目标来自源文件内容并随扫描同步，首次归属的影片番号只从源文件名解析，不从目标地址推测。它不构成独立的资源类型，仍按实际目标表现为视频直链、网页链接、Magnet 或 ED2K，同时保留源文件身份并参与本地文件扫描生命周期。
_Avoid_：一次性链接导入、STRM 本地视频、脱离源文件的链接副本

**STRM 资源身份**：
STRM 影片资源由其绑定的源文件稳定标识；源文件中的目标地址或对应链接类型发生变化时，仍是同一条影片资源，并保留原有归属与主资源身份。不同源文件即使目标地址相同也分别形成资源，且可与相同目标的普通手动链接并存；普通手动链接彼此仍按目标地址去重。
_Avoid_：目标链接即身份、目标变化即新资源、重新选择主资源

**规范化链接目标**：
影片资源用于比较链接目标的稳定值；HTTP/HTTPS 去除首尾空白与片段并保留查询参数，Magnet 按 BTIH 标识，ED2K 按文件哈希判断。
_Avoid_：链接原文、忽略查询参数、显示名称身份

**有效 STRM 源文件**：
不超过 1 MiB、使用 UTF-8 文本（允许 BOM），忽略空行及以 `#` 开头的行后只含一个有效目标地址的 `.strm` 文件；目标只接受影片资源已支持的 HTTP/HTTPS、Magnet 与 ED2K 地址，不接受本地路径或其它协议，超限内容不截断解析。
_Avoid_：多目标播放列表、首行猜测、自定义协议入口

**STRM 同步失败**：
已绑定的 STRM 源文件仍存在但当前内容无法形成有效目标时的可恢复状态；影片资源继续保留上一次成功同步的目标，扫描报告失败，源文件恢复有效后再继续同步。
_Avoid_：清空旧目标、按源文件缺失清理、采用部分内容

**STRM 导入失败**：
尚未形成影片资源的 STRM 源文件因内容无效而无法导入的扫描结果；它不进入待确认扫描组，优先展示路径与内容错误并在后续扫描重试，内容恢复有效后才继续判断文件名番号。
_Avoid_：待确认扫描资源、创建空链接资源、内置 STRM 编辑器

**STRM 失败披露**：
STRM 导入失败与同步失败只持久化或记录源文件路径及分类原因，不包含源文件原文、完整目标地址、查询参数或认证信息；需要辨认目标时最多展示脱敏链接。
_Avoid_：原文错误日志、完整目标持久化、认证信息披露

**STRM 单文件失败**：
一个 STRM 源文件无效、超限或读取失败时，只保留或跳过该项并继续其它资源的安全扫描与缺失清理；只有无法可靠判断目录或路径状态及事务失败等全局错误才阻止清理。
_Avoid_：单个坏文件阻塞整库清理、失败时移除旧资源、忽略全局不安全错误

**完成但有失败项的扫描**：
媒体库扫描已完成安全清理，但存在一个或多个隔离的 STRM 单文件失败；它不是整次扫描失败，自动扫描调度视为已完成，同时保留失败计数与最近一次的受限明细供用户处理。
_Avoid_：完全成功、整次扫描失败、立即无限重试

**STRM 目标维护**：
有效 STRM 源文件是其资源目标地址与链接类型的唯一权威来源，应用内只读展示并由扫描同步，不反向改写源文件；用户可独立维护资源的显示名称与远端内容标称大小，目标同步不得覆盖这两个值，源文件自身大小不作为远端内容大小。
首次导入时显示名称取完整源文件名，标称大小留空且不访问远端探测。
_Avoid_：应用内修改 STRM 目标、数据库覆盖源文件、同步清空展示信息

**STRM 目标同步**：
每次媒体库扫描都重新读取 STRM 源文件并比较规范化链接目标，不以源文件修改时间或大小决定是否读取；有效目标变化时原地同步，无变化时保持资源不变。
_Avoid_：修改时间缓存、文件大小缓存、原始文本比较

**STRM 源文件重定位**：
扫描确认旧源路径缺失后，只有新发现的 `.strm` 文件以完整文件名与规范化链接目标唯一匹配原资源时，才将原 STRM 资源重新绑定到新路径；文件重命名不视为同一源文件，按新资源与旧资源缺失分别处理。
_Avoid_：按文件大小重定位、改名后自动续接、仅按目标链接合并

**STRM 番号归属**：
STRM 源文件名中的番号只用于首次发现或重命名后的新发现；资源绑定后以源路径识别，影片番号纠正不改写源文件，也不因文件名仍含旧番号而重新分配资源。
_Avoid_：每次扫描按文件名重归属、纠正番号即改文件名、旧文件名覆盖影片番号

**STRM 源文件缺失**：
可访问的已配置目录中，绑定源文件确认不存在且未发生 STRM 源文件重定位的状态；扫描按本地影片资源的安全规则移除对应 STRM 影片资源并处理主资源与无资源影片，离线目录及取消或失败的扫描则保留资源。目标链接不可访问不构成源文件缺失。
_Avoid_：远端失效、离线目录缺失、扫描失败清理

**STRM 资源删除**：
用户删除 STRM 影片资源或其所属影片时，同时按本地影片资源的安全删除流程移除绑定的 `.strm` 源文件，失败时不得只提交一侧；删除永远不访问或移除远端内容。
_Avoid_：仅删资源记录、删除远端内容、失败后半删除

**STRM 资源操作**：
打开或播放 STRM 影片资源及复制链接都面向当前目标地址，“在文件夹中显示”则定位绑定源文件；应用不提供打开或编辑源文件内容的影片资源动作。
用户可主动检测 HTTP/HTTPS 目标，检测不属于扫描且不改写源文件，探测到的大小只有经用户保存才成为标称文件大小；Magnet 与 ED2K 不检测。
_Avoid_：播放 STRM 文本、复制源路径、应用内编辑源内容

**主影片资源**：
一部影片当前默认打开的影片资源；有资源的影片至多有一个主影片资源，任意影片资源类型都可以成为主影片资源，主影片资源失效或移除时只按明确顺序选择剩余的可用资源，不静默尝试打开备用资源。
_Avoid_：主文件、默认本地文件、自动故障转移

**扫描主资源选择**：
扫描为新影片分配多条资源时的默认主影片资源选择；存在本地视频时先按时长、大小与路径稳定选择，否则按视频直链、Magnet、ED2K、网页链接及源路径选择 STRM 资源，待确认扫描组允许用户覆盖。
_Avoid_：扫描顺序主资源、STRM 优先于本地视频、不可覆盖默认值

**影片拆分**：
用户将任意一条非主影片资源原子地移入一部新身份待定影片的操作；新影片获得独立内部 ID 并沿用原影片的规范化番号，本地、直链、网页、Magnet 与 ED2K 资源均可拆分。新影片只含该番号和被移动后成为主资源的资源，不继承原影片的资料（含相关链接）、媒体资源、关系或累计刮削状态；本地资源与 STRM 资源只改变数据库归属及源文件绑定，不移动、重命名或改写磁盘文件。
_Avoid_：复制影片资源、拆分主影片资源、只拆分本地文件

**影片合并**：
用户确认两条规范化番号相同的影片代表同一部影片后，将其收敛为一条记录；用户明确选择保留影片，其内部 ID 保持不变，来源影片的资源及 STRM 源文件绑定迁入后删除来源记录。任一影片存在待确认影片刮削结果时通常不得合并，不同番号须先纠正，最终业务身份会与第三部影片冲突时整次阻止。
_Avoid_：自动选择保留影片、番号相同即合并、创建第三条影片

**影片合并资料收敛**：
合并时已有主影片资源优先保留，否则接管来源主资源或稳定选择；单值元数据只补空，用户维护关系取并集且同名手动标签优先，相关链接按去片段 URL 取并集且保留侧优先，封面、海报、样张与刮削标签按完整字段补齐，站点记录按来源取并集且同来源以保留影片为准。累计刮削状态取较强历史，最近刮削时间取较新有效值，合并本身不产生刮削时间。
_Avoid_：来源覆盖、集合混合、伪造刮削时间

**刮削冲突合并**：
用户选择影片刮削候选后触发影片身份冲突，并明确确认进入的特殊影片合并；冲突对方不得持有待确认影片刮削结果，用户选择保留影片后，系统原子地完成合并并按原字段与更新模式应用已选候选，成功后清理待确认结果与未选资源。取消或任一步失败时，影片与待确认结果均保持原状。
_Avoid_：先应用后合并、隐式丢弃候选、部分提交

**媒体库扫描**：
将已配置目录中的磁盘文件与本地影片资源及 STRM 影片资源的源文件进行核对的维护过程；普通链接影片资源不受扫描改变，STRM 影片资源则随源文件及其内容同步。不可访问的已配置目录属于离线目录，其中的资源不能因本次扫描被判定为缺失。
_Avoid_：链接可用性检查、普通链接资源同步、离线目录清空

**媒体库路径移除**：
用户停止管理一个已配置目录的操作；安全清理移除该目录下本地视频与 STRM 源文件对应的资源记录并处理主资源和无资源影片，但不删除任何磁盘文件，路径重新加入后按新发现资源处理。
_Avoid_：删除目录文件、保留 STRM 资源记录、重新加入即恢复旧资源身份

**扫描最小时长**：
媒体库扫描用于跳过较短本地视频文件的导入门槛；STRM 源文件不参与该筛选，也不为此访问远端探测时长。
_Avoid_：STRM 文件时长、远端时长探测、链接导入门槛

**扫描资源重定位**：
扫描发现新路径且旧本地资源已确认缺失时，对同一规范化番号下目标资源的识别；只有一部同番号影片时按文件大小唯一匹配，存在多部时按完整文件名与文件大小唯一匹配，无法唯一命中则按普通新资源处理。离线目录中的旧资源不参与重定位判断。
_Avoid_：修改时间身份、内容指纹、任取缺失资源

**无资源影片**：
当前没有任何影片资源但仍保留元数据的影片；只有用户明确移除最后一个资源并选择保留，或安全扫描后的可选清理尚未执行时才允许存在。
_Avoid_：无文件影片、损坏影片、必删影片

**媒体资源引用**：
数据库中保存的、指向媒体资源的相对路径；引用本身不是文件操作能力。
_Avoid_：绝对路径句柄、文件操作入口

**影片图片字段写入**：
影片封面和样张只有在所需图片全部成为可用本地资源后才整体写入；站点未返回图片属于空结果，返回图片但下载失败或文件不可读属于资源不可用，后者不得清空或部分替换已有资源。空字段补齐与缺失字段筛选只看是否已有媒体资源引用或远程链接，不探本地文件是否可读。
_Avoid_：下载失败清空、部分样张写入、按文件健康判断缺失

**影片刮削应用结果**：
影片刮削匹配后对所选字段的实际处置；空字段补齐或有值覆盖没有可写入内容时属于跳过，不改变刮削状态和时间，覆盖更新的权威清空属于成功应用。用户已选择待确认候选但最终没有可应用内容时结束待确认并清理暂存资源，累计状态仍不改变。
_Avoid_：匹配即成功、无值补齐成功、下载失败清空成功

**影片刮削候选**：
影片刮削插件从搜索结果第一页定位并抓取的番号完全匹配详情；没有候选表示未找到，唯一且不触发影片身份冲突的候选可直接应用，多个候选或唯一冲突候选进入待确认影片刮削结果。
_Avoid_：模糊搜索结果、未抓取详情的链接、第二页结果

**待确认影片刮削结果**：
一次影片刮削中出现多个番号完全匹配候选，或唯一候选触发影片身份冲突时，等待用户处理的一组候选资料、全部暂存资源及发起时的刮削来源、字段与更新模式快照；它独立于影片累计刮削状态和插件当前配置，原插件更新、删除或组合变化不阻止确认。持有结果的影片不得直接或通过实体合并、删除等关系维护间接改写或清除元数据，须先选择或丢弃候选，但实体自身资料、影片资源和相关链接仍可维护；每部影片至多保留一份当前结果且不自动过期，只有新的有效候选结果或成功应用的唯一候选才能取代它，失败或无候选的再次刮削保留旧结果，目标影片被手动删除或自动清理时随影片一并清理。
_Avoid_：第四种累计刮削状态、部分应用候选、自动选择候选

**影片刮削暂存资源**：
待确认影片刮削结果形成时为各候选一次性下载并持久保存、用于辨识候选的全部已声明且通过校验的远程资源，不受本次所选刮削字段限制；未声明或未通过校验的字段与资源不展示、不暂存也不应用。下载失败沿用普通影片刮削的保留与警告行为，选定候选时只使用已暂存资源且仅按原选择字段应用，未用于应用的暂存资源随结果处置清理，不重新访问远端下载。
_Avoid_：仅暂存所选字段、仅暂存封面、确认后重新下载、影片正式媒体资源

**影片候选丢弃**：
用户明确确认一份待确认影片刮削结果中的候选均不正确并放弃整组结果；它清理全部暂存资源并记为本次刮削失败，但不使已有累计成功状态降级，暂不处理不属于丢弃。
_Avoid_：关闭确认界面、暂后处理、成功状态降级

**批量刮削待确认项**：
批量影片刮削中已形成待确认影片刮削结果的项目；它不计成功或失败，不暂停队列且恢复任务时不重复刮削，并在进度与摘要中单独计数。已有待确认结果的影片默认不进入后续新建批量任务，只能由用户从影片详情主动重新刮削并替换。
_Avoid_：批量失败项、暂停点、恢复后重跑项

**组合影片刮削候选选择**：
组合刮削中按来源插件分别选择番号完全匹配候选的确认过程；无歧义来源的结果也随整组暂存，所有歧义来源完成选择后才按组合字段配置合并并一次性应用，不按番号自动跨来源配对。
_Avoid_：跨站点自动配对、候选笛卡尔积、逐来源写入

**待确认演员刮削结果**：
演员刮削返回了资料，但其中存在跨演员的名称归属冲突；用户确认前，整份结果暂不写入任何演员资料。每位演员至多保留一份当前待确认结果，后续刮削以新结果取代旧结果；新结果不再冲突时直接应用并移除旧结果。
_Avoid_：部分成功结果、刮削失败结果

**可应用刮削结果**：
等待期间因名称归属变化而不再包含任何名称冲突项、但尚未由用户明确应用或丢弃的刮削结果；它仍计入待确认，系统不得因冲突自然消失而后台应用资料。
_Avoid_：自动应用结果、刮削成功结果

**演员刮削暂存资源**：
由待确认演员刮削结果暂时持有、尚未成为演员正式头像或写真的资源；结果应用时才晋升，结果被替换、丢弃或失去目标演员时随之清理。
_Avoid_：演员正式资源、永久缓存

**非法名称处理**：
用户在处理当前聚合冲突时，确认冲突文本不应作为演员名称；该决定删除文本的现有名称归属并从本组待确认结果中排除，但不保存为长期规则或错误记录，未来再次出现时仍形成新冲突；若它正是演员主名，必须先由用户明确指定替代主名。
_Avoid_：非法名称黑名单、非法名称标记、插件异常记录

**冲突处理决定**：
用户对待确认演员刮削结果作出的处置，包括确认名称归属、修改名称、合并演员或执行非法名称处理；资料实际应用后记为刮削成功，确认匹配错人并丢弃后记为本次刮削失败。
_Avoid_：自动冲突处理、自动演员合并

**错误匹配丢弃**：
用户确认一份待确认演员刮削结果匹配了错误对象并放弃整份结果；它只影响该次结果，不改变名称归属或同组其他结果。
_Avoid_：聚合组丢弃、名称归属决定、部分资料保留

**演员头像**：
演员当前已保存且文件可用的头像；路径为空、文件丢失或图片不可读都不算有头像。头像源、候选头像和单独的裁剪参数也不构成演员头像。
_Avoid_：头像源、候选头像、头像裁剪参数

**头像人脸检测状态**：
当前演员头像经本地人脸模型检测得到的分类，包含“有脸”“无人脸”“检测失败”和“待检测”；“无人脸”仅表示推理成功但没有检测到人脸。
_Avoid_：人脸识别结论、头像质量评分

**无人脸头像**：
当前可用头像已完成本地人脸检测，但没有检测到任何人脸；它不等同于无头像，也不包含图片读取或模型运行失败的记录。
_Avoid_：无头像、识别失败头像

**图片预览未缩放状态**：
图片预览以 100% 比例显示、尚未进入画布拖拽状态的交互状态；只有该状态允许通过水平拖动切换前后图片。
_Avoid_：任意缩放比例、画布拖拽状态

**图片预览切换手势**：
用户在图片预览未缩放状态下进行明显的水平拖动，用于切换上一张或下一张图片；短距离拖动、以垂直方向为主的拖动和边界外切换都不产生切换。
_Avoid_：缩放后平移、普通点击、缩略图拖动
