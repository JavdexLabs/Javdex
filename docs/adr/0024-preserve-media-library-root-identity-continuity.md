---
status: accepted
---

# 保持媒体库根目录的物理身份连续性

媒体库根目录一旦持有影片资源、待确认资源或无法识别项，Javdex 就以已确认的物理目录身份持续绑定这些记录，而不把后来占用相同路径的目录自动视为原根；重新启用、恢复归档和破坏性清理都必须重新验证身份，普通路径编辑也不能转移已有归属。相比按路径自动重绑，这会要求用户恢复原挂载或执行显式迁移/清理，但可以避免可移动磁盘、网络挂载和替换目录悄然继承或删除错误数据；取消尚未完成的路径移除因此只撤销清理任务并将根目录转为停用，同时保留原身份和全部记录。
