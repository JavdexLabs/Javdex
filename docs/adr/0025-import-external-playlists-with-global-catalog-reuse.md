---
status: accepted
---

# 用独立 Agent 原子导入外部清单并复用全局影片目录

外部清单导入使用独立的 `playlist-importer` 用例和 `PlaylistImportModule`，统一支持新建清单与追加到已有清单。任务先把全部清单页和有限虚拟滚动窗口固化到暂存表，枚举完成后才读取必要的影片详情；全部影片身份确定后，在单个 SQLite 事务中按任务冻结的写入选项创建或复用影片、保存详情相关链接并写入清单。Agent 只能提交页面和身份证据，不能直接修改影片、媒体库或清单。“自动创建无资源影片”和“保存详情页到相关链接”默认开启，关闭后分别跳过自动新建条目或不写入详情链接；“保存来源清单链接”默认关闭，开启后把外部清单 URL 去重追加到目标清单的相关链接。

新清单名称留空时，Agent 在首个清单页检查点中只能根据明确页面证据提交名称建议；不能根据影片内容、URL 或用户名编造。最终原子应用依次使用用户输入、Agent 名称建议和原有的“域名 · 日期”兜底。

影片候选在全局目录中按永久影片 ID 去重。唯一规范化番号完全匹配是本导入用例的快速复用策略，不是全局影片唯一性约束；多个同番号影片必须用详情强信号进一步匹配。强信号仍无法唯一选择时，目标媒体库只在恰好包含一个合理候选时作为决胜偏好，否则交给用户。复用其它媒体库影片不会迁移或复制成员关系；目标媒体库仅承接新建的无资源影片。

清单是全局对象，可以引用跨媒体库影片或没有媒体库成员的影片。因此清单读取不按活动媒体库过滤，而且只要任一清单仍引用影片，自动的无资源成员清理和全局无资源影片清理都必须保留它。用户显式移除成员、清单引用或影片仍按原有权限执行。

这个决策增加了专用暂存表、恢复和身份决策状态，但换取页面级完整性、最终原子性和可审计重试；它也避免把站点分页、选择器、跨库匹配和逐条写入分散到 UI、IPC 与通用 Agent prompt 中。
