# AM static review

Baseline4c953a9, cumulative working tree after AL. Both reviewers read only and did not run tests or benchmarks.

## Standards — Anscombe

No concrete blocker: bounded safe integer existence API, selected audit-page lookup, one snapshot and explicit unknown/error, shared Button and established styles. Linear first-occurrence deduplication; all-to-failed page navigation retained. Full audit files/scan identity arrays were not claimed complete.

## Spec — Ptolemy

Initial P2: Aready→Bpending→A could reuse the earlier ready snapshot before the new lookup returned. Fixed using an object identity per key transition, and exact ABA regression. Final reviewer confirmed old A cannot match new request and late B cannot publish. Earlier focus-evidence gap was subsequently covered by final browser script document.activeElement and in-viewport bounding-box assertions at item201; reviewers did not execute those checks themselves.

## Evidence limits

Final12 hook/component tests, full2199pass1skip, build and final Chrome fixture pass. Browser uses actual audit component/styles with simulated IPC, not complete settings-page startup or native application. Local 100-row pagination does not bound initial full audit parsing/filtering or outstanding stale IPC work globally. Historical counts remain whole filtered arrays; not counts of currently unresolved work.
