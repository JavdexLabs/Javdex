# BK 静态与测量复核

Standards/Spec均无具体阻断。SQL trim集合匹配ECMAScript，BLOBlen保留NUL；正尺寸且计算比值有限并>1才优先，position null按0然后id；同事务count/page、排序先于LIMIT。891组合完整字段oracle、WAL与边界覆盖主要合同。

测量静态复核确认：旧准备数组每次完整字段等值，首末页与其切片逐字段对照；断言/JSON在计时之外。旧JSON仅准备后的gallery，新JSON包含页包装，不是原完整metadata IPC等体积比较。仅捕获首页页面EQP，非count或末页单独计划。

5万记录4万有效图库旧26.028ms/16,338,225B，首23.613ms/24,383B，末47.441ms/24,591B；深页更慢，返回体更小，不支持整体提速。3热样本，固定旧先新后，oracle驻留；无UI/真实IPC/p95/冷盘/目标平台/峰值内存结论。长路径保留，不是字节预算已完成。

31定向通过；全量2314通过、1跳过、0失败，build通过。探针1通过。未编辑用户数据/媒体、迁移schema、提交或推送。
