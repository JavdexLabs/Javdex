# DD/DE 收尾静态复核

用户将目标收敛为完成NFO相关优化、更新完整计划后停止。此次只收尾扫描预检计数及NFO工作集，不执行其余性能工作包，也不宣称完整15/42验收。

## 已关闭的问题

- TEMP入队若发生在资源提交后，存储失败可能使重试跳过已有资源。最终四条summary新增/附加local/STRM成功分支把入队、资源和基础file audit放在同一事务内；计数/primary集合在提交后更新。enqueue的序号完全由SQLite管理，允许嵌套保存点；其他变更仍在状态修改前拒绝调用方事务。原生故障测试确认故障发生前资源与审计均存在、故障后当前文件回滚、之前提交保留，并实际重试导入NFO。
- for-of会先hydrate下一作品再检查取消。最终改为next前检查信号，并在finally关闭iterator；删除队列表的取消回归证明取消后没有下一批SQL读取，同时当前批次警告审计完成。
- 目录包装错误丢失原始cause。最终保留至原生SQLite的错误链；工作集错误穿透inspectIdentity、file和apply warning降级路径。
- detailed返回的sidecar快照过去可在扫描结束后继续使用。最终内存后端提供真实Map保留该合同；summary采用仅在扫描生命周期内有效的SQL查询视图。
- memory兼容后端逐次从头寻找ordinal会退化为二次遍历，已改为按ordinal直接定位；summary查询使用对应索引和完成的get，不保留活动SQLite游标。
- 最终全量检查首次发现keys/values生成器返回类型与MapIterator不匹配，父任务补齐显式返回类型后重新运行完整检查；没有更改运行逻辑。

独立审查者最后确认当前审查范围没有剩余阻断；审查者未运行测试。最终联合测试、全量和构建由主任务确认实际终端退出码，记录于manifest和实施记录。

## 明确保留的边界

一次恢复单作品anchors并沿用一次apply，保留多候选判定与业务/审计事务语义；单作品数组和候选仍可能很大。瞬时目录枚举/Map、同步目录写入时长、TEMP总量、编码开销及真实平台峰值不在固定总预算保证内。TEMP不是进程崩溃后的恢复账本；这里只保证当前事务内入队失败的原子性。

DD最终复用语句的计数探针可用当前脚本复跑；早期未缓存版本仅保留迭代测量原始日志，未单独冻结初版源码，不能将该前后比较作为独立发布版本的回归基线。最终同一源码内memory/TEMP两后端对照有当前代码快照；都不代表整扫描、RSS、p95或Windows/HDD验收。
